name: test-contract
on:
  push:
  pull_request:

jobs:
  test-contract-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run testing-contract guard
        run: |
          chmod +x .opencode/commands/guard.sh
          ./.opencode/commands/guard.sh
