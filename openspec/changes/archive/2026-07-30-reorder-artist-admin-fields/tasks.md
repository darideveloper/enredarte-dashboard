## 1. Update ArtistAdmin Layout

- [x] 1.1 Add `fieldsets` to `ArtistAdmin` in `artworks/admin.py` to organize fields into logical sections (e.g., Personal Info, Contact & Media, System Status), ensuring `slug` is placed immediately after `name` on the first row.

## 2. Verify

- [x] 2.1 Run Django tests or start the local dev server to ensure the `ArtistAdmin` form loads correctly without configuration errors.
