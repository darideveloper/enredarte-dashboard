## 1. Fix BillingPlan live preview (C1) + thread-safety (N3)

- [x] 1.1 Edit `subscriptions/admin.py:112-130` `BillingPlanAdmin.change_view`: replace `if isinstance(recurring,dict): interval=recurring.get(...) elif recurring: interval=sget(...)` branch with uniform `interval = sget(recurring,"interval","") or ""` after `recurring = sget(price,"recurring",None)`; store via `request._stripe_live_summary` (not `self._stripe_live_summary`) and read `display_stripe_live` from `request`; narrow `except Exception` to `except stripe.error.StripeError as e: logger.warning` + generic `except Exception: logger.warning` fallback showing `"(no se pudo confirmar)"`.
- [x] 1.2 Add `subscriptions/tests.py` `LivePreviewTest.test_shows_confirmed_with_stripe_object`: use `stripe.Price.construct_from({"id":"price_test","unit_amount":29900,"currency":"mxn","recurring":{"interval":"month"}}, key="sk_test")` with `patch("subscriptions.services.stripe_client.retrieve_price",...)` assert `Confirmado por Stripe: 299.00 MXN / month` (proves `sget` path, no `AttributeError`).

## 2. Harden ArtistAdmin Stripe error handling (W1) + single-save + customer unwrap (N1/N2/S3)

- [x] 2.1 Edit `artworks/admin.py:388-430` `generate_link`: wrap `create_customer` + `create_checkout_session` in `try/except stripe.error.StripeError as e: messages.error(request, f"Stripe no respondió: {e}"); logger.warning("generate_link artist=%s StripeError: %s", artist.pk, e); return redirect(redirect_url)`; ensure `customer_email` set via `sget(customer,"email") or artist.email` before save; no partial `ArtistSubscription` link on failure.
- [x] 2.2 Edit `artworks/admin.py:432-475` `regenerate_link`: same `try/except` around `create_customer`/`create_checkout_session`; `expire_or_reuse_session` path stays `302 MSG_LINK_REGENERATED` without Stripe call; log warning on failure.
- [x] 2.3 Edit `artworks/admin.py:477-488` `open_portal`: wrap `create_billing_portal_session` in `try/except StripeError` → `messages.error` + `302`; log `warning`.
- [x] 2.4 Edit `artworks/admin.py:490-525` `sync_from_stripe`: set `sub.customer_email = sget(customer,"email") or sub.customer_email` before `apply_stripe_payload` (via `sget` not `customer.email`); remove double-save at `513-514` (`sub.save(update_fields=["customer_email","updated_at"])` after `apply` already saved 8 fields), keep single save inside `apply`; unwrap expanded `customer` in `subscriptions/models.py:274` `apply_stripe_payload`/`upsert_from_stripe`: `cust = sget(stripe_sub,"customer"); if isinstance(cust, dict): cust_id = sget(cust,"id", cust)` then `self.stripe_customer_id=cus_id`; wrap `fetch_customer` + `list_subscriptions` in `try/except StripeError` → `messages.error` + `302` + `logger.warning(message="sync artist=%s ...", artist.pk)`; no DB mutation on failure (verify `sub.status`/`is_active`/`last_synced_at` unchanged).
- [x] 2.5 Add `subscriptions/tests.py` 4 `AdminEndpointTest` StripeError tests: `test_generate_link_stripe_error_on_create_customer`, `test_regenerate_link_stripe_error`, `test_open_portal_stripe_error`, `test_sync_from_stripe_stripe_error_on_fetch_customer` + `test_sync_from_stripe_stripe_error_on_list_subscriptions` — each patches `side_effect=stripe.error.StripeError("boom")` asserts `302`, `messages.error "Stripe no respondió"`, DB unchanged, `logger.warning` called.

## 3. Observability (W3) + version pin + startup check (S1)

- [x] 3.1 Edit `project/settings.py` add `LOGGING = {"version":1,"disable_existing_loggers":False,"handlers":{"console":{"class":"logging.StreamHandler"}},"loggers":{"django":{"handlers":["console"],"level":"INFO"},"subscriptions":{"handlers":["console"],"level":"INFO","propagate":False},"artworks":{"handlers":["console"],"level":"INFO","propagate":False}}}`; pass-through `SENTRY_DSN` if env.
- [x] 3.2 Edit `subscriptions/webhooks.py:150-193` add `import logging; logger=logging.getLogger(__name__)`; `logger.info("webhook %s %s", event_type, event_id)` on success, `logger.exception("webhook %s failed", event_dict["id"])` on handler crash (outside rollback, log survives); ensure `logger.warning` on invalid signature/missing header.
- [x] 3.3 Edit `subscriptions/apps.py` add `ready()` `system_check`: `if os.getenv("ENV","dev")!="dev" and (not settings.STRIPE_SECRET_KEY or not settings.STRIPE_WEBHOOK_SECRET.startswith("whsec_")): raise ImproperlyConfigured("STRIPE_* missing")` + `if settings.STRIPE_API_VERSION==""` warning. Edit `requirements.txt:29` pin `stripe>=15.5.1,<16`.

## 4. Plan sync orphan warning (W4)

- [x] 4.1 Edit `subscriptions/services/plan_sync.py:58-62` wrap `archive_price(old_price_id)` in `try/except stripe.error.StripeError as e: logger.warning("orphan price new=%s old=%s StripeError: %s", new_price_id, old_price_id, e); raise`; add doc comment `"""Retry creates another price, orphan remains in Stripe until manual archive."""` before `transaction.atomic` at `65`. Keep Stripe calls outside inner `atomic` (no DB change on failure). Add inline `logger.info("plan_sync price %s -> %s amount %s", old_price_id, new_price_id, plan.amount)` after `History` creation.

## 5. Batch small guards + new handler (W5/S4 + N5/N1 extras)

- [x] 5.1 Edit `subscriptions/webhooks.py:39-45,103-110` `_invoice_period_end` guard: `period_end = epoch_to_datetime(...); if period_end is not None:` only then `sub.current_period_end = period_end` in `_handle_invoice_payment_succeeded` (avoid wiping valid period on empty `lines.data`); keep `sub.status=ACTIVE`, `raw_state=to_plain_dict(invoice)`.
- [x] 5.2 Edit `subscriptions/webhooks.py:HANDLERS` add `checkout.session.expired`: define `_handle_checkout_expired(event)` that resolves `artist_id = sget(session,"metadata/artist_id")` or `sget(event["data"]["object"],"metadata/artist_id")`, finds `ArtistSubscription` by `artist_id`, if stored `signup_url` equals `sget(session,"url")` or fallback match by `metadata.artist_id`, clears `signup_url=""`/`signup_url_expires_at=None` + `last_synced_at=now()` + `logger.info("checkout expired artist=%s", artist_id)` (status stays as-is, typically `pending`, no explicit status change); register in `HANDLERS` + `logger.info`.
- [x] 5.3 Verify `subscriptions/models.py:274` customer unwrap and `artworks/admin.py:502` `sget(customer,"email")` — already covered in 2.4; no separate edit, only verification that `sget(customer,"email")` is used and expanded `{"id":...}` unwrap stores `sget(cust,"id")` not dict string.
- [x] 5.4 Edit `docs/stripe-subscriptions.md:132-140` + `openspec/specs/stripe-webhook-handler/spec.md:37-45` align to keep-rollback: replace `StripeEvent row outside transaction MUST still record error` with `StripeEvent row is created inside single atomic together with handler; on handler crash entire transaction including StripeEvent is rolled back, endpoint returns 500 for Stripe retry (retry is fresh); error is logged via logger.exception, not persisted`.

## 6. Edge tests (gap7 batch)

- [x] 6.1 Add `subscriptions/tests.py` `WebhookEdgeTest` 2 tests: `test_invoice_payment_succeeded_with_empty_lines_keeps_period` (invoice `lines:{"data":[]}` → `current_period_end` unchanged, `status=ACTIVE`), `test_map_stripe_status_unknown_returns_pending` (`make_subscription(status="weird_xyz")` → `PENDING` via `map_stripe_status` → `compute_is_active=False`).
- [x] 6.2 Add `subscriptions/tests.py` `test_checkout_session_expired_clears_signup_url`: create `pending` `ArtistSubscription` with `signup_url`, deliver `checkout.session.expired` via `stripe_signature` → assert `signup_url==""`, `status` still `pending`.

## 7. Verification

- [x] 7.1 Run `python manage.py test subscriptions -v 2` (expect `73→~83` tests, new: 4 StripeError admin + 1 LivePreview StripeObject + 2 webhook edges + 1 map_status + 1 expired + 1 plan_sync log) — all green, no `TypeError: Decimal...`/`AttributeError: 'get' is a dict method`/`KeyError 0`.
- [x] 7.2 Manual prod check after deploy: `GET /admin/subscriptions/billingplan/1/change/` → `Confirmado por Stripe: …` (C1), `GET /admin/artworks/artist/5/sync-from-stripe/` → `302 Suscripción sincronizada` (W1), inject `side_effect=StripeError` in dev → `messages.error` + `302`, `POST /webhooks/stripe/` with `checkout.session.expired` → expired link cleared.
