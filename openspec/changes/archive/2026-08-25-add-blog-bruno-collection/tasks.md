## 1. Bruno Collection Setup for Blog

- [x] 1.1 Create `bruno/collections/enredarte-dashboard-api/Posts/GET list.bru` (`seq: 21`) with full request config and `docs` block for paginated summary listing
- [x] 1.2 Create `bruno/collections/enredarte-dashboard-api/Posts/GET detail.bru` (`seq: 22`) with full request config and `docs` block for detail post retrieval by slug
- [x] 1.3 Update `bruno/README.md` to document the new `Posts` folder and its public endpoint behavior

## 2. Verification

- [x] 2.1 Verify `.bru` files syntax, sequence numbering, and validate change with `openspec validate --changes add-blog-bruno-collection`
