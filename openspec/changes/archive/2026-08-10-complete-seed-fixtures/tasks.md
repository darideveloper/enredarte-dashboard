## 1. Renumber existing seed fixtures

- [x] 1.1 Rename `artworks/fixtures/artworks/seed/Artist.json` → `02_Artist.json`
- [x] 1.2 Rename `artworks/fixtures/artworks/seed/ArtistSocialLink.json` → `03_ArtistSocialLink.json`
- [x] 1.3 Rename `artworks/fixtures/artworks/seed/ArtistTranslation.json` → `04_ArtistTranslation.json`
- [x] 1.4 Rename `artworks/fixtures/artworks/seed/Artwork.json` → `05_Artwork.json`
- [x] 1.5 Rename `artworks/fixtures/artworks/seed/ArtworkTranslation.json` → `10_ArtworkTranslation.json`

## 2. Add curators and galleries fixtures

- [x] 2.1 Create `00_ArtCurator.json` with 2 curators (explicit PKs 1–2, Person fields, unique slugs)
- [x] 2.2 Create `01_ArtCuratorTranslation.json` with es+en `bio` for both curators
- [x] 2.3 Create `06_Gallery.json` with 2 galleries (PKs 1–2) referencing curator PKs via `curator` (slug optional — auto-backfilled from the ES translation name, or supply it explicitly for determinism)
- [x] 2.4 Create `07_GalleryTranslation.json` with es+en `name` + `description` for both galleries

## 3. Add artwork gallery links and images fixtures

- [x] 3.1 Create `08_ArtworkGallery.json` with ~8 links across the 6 seeded artworks and 2 seeded galleries, each with a unique `slug` (explicit, or auto-generated by the model's `save()`)
- [x] 3.2 Create `09_ArtworkImage.json` with 1 image per seeded artwork (each marked `is_primary=True`, `slug` explicit or auto-generated, paths `artworks/obra-<n>.jpg` matching the committed files)

## 4. Seed images and loader sync

- [x] 4.1 Commit 6 small placeholder JPGs at `artworks/fixtures/artworks/seed/images/artworks/obra-1.jpg` … `obra-6.jpg` (one per seeded artwork)
- [x] 4.2 Add a pre-load media-sync step in `core/management/commands/seed_loaddata.py` that writes each app's `fixtures/<app>/seed/images/` files into the configured default storage via `default_storage.exists()`/`save()` (covers local `MEDIA_ROOT` and S3), skipping files that already exist

## 5. Tests

- [x] 5.1 Add a seed-completeness test asserting `seed_loaddata` leaves no business table empty (ArtCurator, Gallery, ArtworkGallery, ArtworkImage included)
- [x] 5.2 Add a re-run test asserting `seed_loaddata` twice does not increase row counts
- [x] 5.3 Run `python manage.py test` and ensure the full suite passes

## 6. Docs

- [x] 6.1 Update the seed-file inventory and `seed_loaddata` example in `docs/django-fixtures.md` to reflect numeric prefixes and the seed-media sync

## 7. Verify end-to-end

- [x] 7.1 On a fresh DB run `base_loaddata` then `seed_loaddata` and confirm every business table has rows
- [x] 7.2 Confirm re-running `seed_loaddata` leaves counts unchanged (rows update in place by PK, no errors)
